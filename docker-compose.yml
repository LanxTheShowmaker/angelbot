services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: wings
      POSTGRES_PASSWORD: wings
      POSTGRES_DB: wings
    ports:
      - "5432:5432"
    volumes:
      - wings_pg:/var/lib/postgresql/data

volumes:
  wings_pg:
